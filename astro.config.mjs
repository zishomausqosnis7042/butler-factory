import { defineConfig } from 'astro/config';

export default defineConfig({
  output: 'static',
  site: 'https://ВАШ-БУДУЩИЙ-АДРЕС.netlify.app' // Мы заменим это на ваш реальный адрес Netlify позже
});
